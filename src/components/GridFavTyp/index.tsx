import React from 'react'
import { TypeChildren } from '../../types/children'
import { Container } from './styles'

export const GridFavTyp = ({ children }: TypeChildren) => {
  return <Container>{children}</Container>
}
